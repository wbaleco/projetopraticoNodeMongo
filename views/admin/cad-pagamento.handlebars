<div class="content p-1">
    <div class="list-group-item">
        <div class="d-flex">
            <div class="mr-auto p-2">
                <h2 class="display-4 titulo">Cadastrar Pagamento</h2>
            </div>
            <div class="p-2">
                <span class="d-none d-md-block">
                    <a href="/pagamentos" class="btn btn-outline-info btn-sm">Listar</a>

                </span>
                <div class="dropdown d-block d-md-none">
                    <button class="btn btn-primary dropdown-toggle btn-sm" type="button" id="acoesListar"
                        data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Ações
                    </button>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="acoesListar">
                        <a class="dropdown-item" href="/cat-pagamento">Listar</a>

                    </div>
                </div>
            </div>

        </div>
        {{>_msg}}
        <hr>
        <form action="add-pagamento" method="POST">
            <div class="form-row">
                <div class="form-group col-md-8">
                    <label><span class="text-danger">*</span> Nome</label>
                    <input name="nome" type="text" class="form-control" id="nome" placeholder="Nome do pagamento">
                </div>
                <div class="form-group col-md-4">
                    <label><span class="text-danger">*</span>Valor</label>
                    <input name="valor" type="text" class="form-control" id="valor" placeholder="Valor do pagamento">
                </div>
            </div>
            <div class="form-row">
                <div class="form-group col-md-12">
                    <label>Categoria de Pagamento</label>
                    <select name="catpagamento" id="catpagamento" class="form-control">
                        {{#each catpagamentos}}
                        <option value="{{_id}}">{{nome}}</option>
                        {{else}}
                        <option value="">Nenhuma Categoria de pagamento encontrado</option>
                        {{/each}}
                    </select>
                </div>
            </div>
            <p>
                <span class="text-danger">* </span>Campo obrigatório
            </p>
            <button type="submit" class="btn btn-success">Cadastrar</button>
        </form>
    </div>
</div>